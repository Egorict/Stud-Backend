generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Place {
  id               String @id @unique
  distance         Float
  name             String
  discount         Float
  duscountDuration Float
}
